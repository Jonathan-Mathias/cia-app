# CIA App — QA run 2026-06-04

## Escopo

Primeira verificação depois da preparação Supabase-ready e pluginização dos emails AU.

## Checks executados

- JavaScript extraído do `index.html` validado com `node --check /tmp/cia-script.js`.
  - Resultado: PASS.
- Servidor local iniciado com `python3 -m http.server 8787`.
  - Resultado: PASS.
- `curl` local confirmou presença de `APP_CONFIG` no HTML servido.
  - Resultado: PASS.
- Chromium headless carregou `http://127.0.0.1:8787/index.html` e gerou DOM.
  - Resultado: PASS.
- Checks estáticos:
  - Supabase permanece desligado por padrão: PASS.
  - Emails AU estão como plugin pendente de revisão: PASS.
  - Emails DE permanecem habilitados: PASS.
  - Texto antigo de “dados armazenados de forma anônima” não existe mais: PASS.
  - Docs de schema Supabase e checklist QA existem: PASS.

## Limitações desta rodada

- Não foi feito teste manual completo clicando todas as respostas no navegador.
- Não foi ativado Supabase real.
- Não foi disparado email real de produção.
- Browser automation interativo do ambiente não aceitou abrir localhost; a validação visual ficou via Chromium headless/DOM.

## Próximo QA recomendado

1. Rodar fluxo Alemanha completo com 4 perfis controlados: Explorador, Em Rota, Pronto, Elite.
2. Rodar fluxo Austrália completo confirmando que resultado aparece, lead salva e email fica pendente.
3. Ativar `APP_CONFIG.email.testMode=true` em branch/teste para validar delays curtos via proxy.
4. Depois que Supabase existir, ativar em ambiente de teste e validar inserts nas 5 tabelas.

## Rodada produto/branding

Após a autorização para transformar o app em produto EML, foram adicionados:

- landing com promessa mais forte: percepção de valor internacional, não quiz genérico;
- Alemanha marcada como rota principal EML e Austrália como segunda rota;
- acento visual da rota Austrália movido para verde profundo, preservando azul como cor funcional;
- CTA principal único por banda no resultado;
- link real para `privacy.html` no consentimento;
- página pública `privacy.html` criada para o MVP.

Checks executados:

- `node --check /tmp/cia-script.js`: PASS.
- Static checks de CTA principal, privacy link, Alemanha-first e AU verde: PASS.
- Chromium headless renderizou `index.html`: PASS.
- Chromium headless renderizou `privacy.html`: PASS.

Limitação: ainda falta QA clicado de todos os cenários/personas dentro do navegador.

## Rodada funcional via Chrome DevTools Protocol

Como o browser interativo do ambiente não abriu localhost, rodei Chromium headless com remote debugging e avaliei a lógica real da página carregada.

Cenários automatizados:

- DE low: score 0, Explorador, CTA `community_foundation`.
- DE mid: score 62, Em Rota, CTA `cv_builder`.
- DE ready: score 68, Pronto para Embarcar, CTA `strategic_application`.
- DE elite: score 100, Elite, CTA `premium_waitlist`.
- AU low: score 0, Explorador, CTA `community_foundation`.
- AU mid: score 67, Pronto para Embarcar, CTA `strategic_application`.
- AU ready: score 66, Pronto para Embarcar, CTA `strategic_application`.
- AU elite: score 100, Elite, CTA `premium_waitlist`.

Resultado: PASS para cálculo, banda e roteamento de CTA principal nos cenários sintéticos.

Observação: o cenário AU mid caiu em Pronto para Embarcar porque as opções médias da rota AU pontuam de forma relativamente alta. Não é bug técnico, mas vale revisar calibragem de score AU quando Jonathan revisar a rota Austrália.

## Visual screenshot review

Captured mobile screenshots for home and privacy pages with Chromium headless.

Findings:

- Home is directionally on-brand, but secondary text contrast can improve.
- Badges are small on mobile and may not carry enough hierarchy.
- Destination cards leave some empty area in the screenshot, not a blocker but worth refining after core flow QA.
- Privacy page works, but still feels more like a policy document than a fully branded EML page. Acceptable for MVP, should be visually integrated later.

Decision: not a blocker for product QA, but add visual polish pass before final publication.

## Rodada pós-limpeza de CTAs secundários

- Removido card secundário fixo do CV Builder para reduzir competição com o CTA principal.
- CTA principal `cv_builder` e `strategic_application` agora preserva payload no `localStorage/sessionStorage` antes de abrir o CV Builder.
- `node --check`: PASS.
- `node tools/qa-cdp.mjs`: PASS.
